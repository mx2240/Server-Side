<h1>All Products</h1>
<ul>
    <% products.forEach(product=> { %>
        <li>
            <a href="/products/<%= product._id %>">
                <%= product.name %>
            </a> - $<%= product.price %> - Rating: <%= product.rating %>
        </li>
        <% }) %>
</ul>